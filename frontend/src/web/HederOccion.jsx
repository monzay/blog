import React from 'react'

export const HederOccion = ({occion}) => {
  return (
            <div className="contenedor-occiones">
              <span className="occion">{occion} </span>
            </div>
  )
}
